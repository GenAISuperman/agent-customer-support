agent:
  id: "customer-support"
  team: "support"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "strict"

identity:
  sp_client_id: "74f384e0-005a-404b-bedf-184dfce538e7"
  keyvault_url: "https://kv-shop-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

prompts:
  active_version: v1.0
  source: prompts/system.txt

apiVersion: v1
kind: Service
metadata:
  name: "customer-support"
  namespace: "agents-support"
spec:
  selector:
    app: "customer-support"
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
